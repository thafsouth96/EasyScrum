{% trans_default_domain 'FOSUserBundle' %}

<div class="panel panel-info" style="margin-top: 20px" >
      <div class="panel-heading">
              <div class="panel-title">Réinitialiser le mot de passe</div>
              <div style="float:right; font-size: 80%; position: relative; top:-10px"><a href= "{{ path('fos_user_security_login')}}">Connectez-vous ici</a></div>
      </div>
<div style="padding-top:30px" class="panel-body" >

    <form action="{{ path('fos_user_resetting_send_email') }}" method="POST" class="fos_user_resetting_request">
      <div>
          <label class="sr-only" for="username">{{ 'resetting.request.username'|trans }}</label>
          <div style="margin-bottom: 25px" class="input-group">
            <span class="input-group-addon"><i class="glyphicon glyphicon-user"></i></span>
            <input type="text" id="username" class="form-control" name="username"  placeholder="Adresse mail" autofocus required />
          </div>
      </div>

      <div style="text-align:center; margin-bottom: 20px " >
          <input class="btn btn-lg btn-primary"  type="submit" value="{{ 'resetting.request.submit'|trans }}" />
      </div>

    </form>
  </div>
</div>
